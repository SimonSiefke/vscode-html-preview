language: node_js

node_js: 12

stages:
  - name: test
  - name: release
    if: branch = master AND type = push AND fork = false

jobs:
  include:
    - stage: test
      script:
        - npm test
        - npm run package
    - stage: release
      script:
        - npm run package
        - cd dist
        - npx semantic-release
