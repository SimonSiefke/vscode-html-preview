language: node_js

node_js: lts/*

stages:
  - name: test
  - name: release
    if: branch = master AND type = push AND fork = false

jobs:
  include:
    - stage: test
      script:
        - npm test
    - stage: release
      script:
        - npm run package
        - cd dist
        - npx semantic-release
